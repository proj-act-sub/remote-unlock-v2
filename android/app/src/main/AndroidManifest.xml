<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- QR scanner -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Network access for Noise tunnel -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Wake lock: keep connection alive while unlocking -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- BiometricPrompt: no separate permission needed (automatically granted) -->

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".RemoteUnlockApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RemoteUnlock">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
